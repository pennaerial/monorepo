GitHub Actions CI assets live here.

- `ros-jazzy-ci/` builds the ROS Jazzy image used by ROS and simulation validation and the ARM artifact workflow.
- The image is built for `amd64` and `arm64` and includes the stable workspace and headless simulation setup.
