name: Publish ROS CI Image

on:
  push:
    branches: [main, master, integration]
    paths:
      - ".github/workflows/ci-image.yml"
      - ".github/ci/**"
      - "controls/sae_2025_ws/scripts/ci/common.sh"
      - "controls/sae_2025_ws/scripts/ci/install_ros_ci_deps.sh"
  workflow_dispatch:

permissions:
  contents: read
  packages: write

concurrency:
  group: ci-image-${{ github.ref }}
  cancel-in-progress: true

env:
  IMAGE_NAME: ghcr.io/${{ github.repository_owner }}/sae-ros2-ci

jobs:
  build_image:
    name: Build ${{ matrix.arch }} CI image
    runs-on: ${{ matrix.runner }}
    strategy:
      fail-fast: false
      matrix:
        include:
          - arch: amd64
            platform: linux/amd64
            runner: ubuntu-22.04
          - arch: arm64
            platform: linux/arm64
            runner: ubuntu-22.04-arm

    steps:
      - name: Checkout repository
        uses: actions/checkout@v4

      - name: Set up Docker Buildx
        uses: docker/setup-buildx-action@v3

      - name: Log in to GHCR
        uses: docker/login-action@v3
        with:
          registry: ghcr.io
          username: ${{ github.actor }}
          password: ${{ secrets.GITHUB_TOKEN }}

      - name: Build and push CI image
        uses: docker/build-push-action@v6
        with:
          context: .
          file: .github/ci/ros-jazzy-ci/Dockerfile
          platforms: ${{ matrix.platform }}
          push: true
          tags: |
            ${{ env.IMAGE_NAME }}:jazzy-${{ matrix.arch }}
            ${{ env.IMAGE_NAME }}:sha-${{ github.sha }}-${{ matrix.arch }}
          cache-from: type=gha,scope=ci-image-${{ matrix.arch }}
          cache-to: type=gha,mode=max,scope=ci-image-${{ matrix.arch }}

  build_sim_image:
    name: Build ${{ matrix.arch }} sim CI image
    needs: build_image
    runs-on: ${{ matrix.runner }}
    strategy:
      fail-fast: false
      matrix:
        include:
          - arch: amd64
            platform: linux/amd64
            runner: ubuntu-22.04
          - arch: arm64
            platform: linux/arm64
            runner: ubuntu-22.04-arm

    steps:
      - name: Checkout repository
        uses: actions/checkout@v4

      - name: Set up Docker Buildx
        uses: docker/setup-buildx-action@v3

      - name: Log in to GHCR
        uses: docker/login-action@v3
        with:
          registry: ghcr.io
          username: ${{ github.actor }}
          password: ${{ secrets.GITHUB_TOKEN }}

      - name: Build and push sim CI image
        uses: docker/build-push-action@v6
        with:
          context: .
          file: .github/ci/ros-jazzy-sim-ci/Dockerfile
          platforms: ${{ matrix.platform }}
          push: true
          build-args: |
            BASE_IMAGE=${{ env.IMAGE_NAME }}:jazzy-${{ matrix.arch }}
          tags: |
            ${{ env.IMAGE_NAME }}:jazzy-sim-${{ matrix.arch }}
            ${{ env.IMAGE_NAME }}:sim-sha-${{ github.sha }}-${{ matrix.arch }}
          cache-from: type=gha,scope=sim-image-${{ matrix.arch }}
          cache-to: type=gha,mode=max,scope=sim-image-${{ matrix.arch }}
