[develop]
script_dir=$base/lib/vehicle_common
[install]
install_scripts=$base/lib/vehicle_common
[tool:pytest]
markers =
    live: requires real hardware or a running peer stack; excluded from default CI
