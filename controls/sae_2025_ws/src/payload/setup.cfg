[develop]
script_dir=$base/lib/payload
[install]
install_scripts=$base/lib/payload
[tool:pytest]
markers =
    live: requires real hardware or a running peer stack; excluded from default CI
junit_suite_name = payload

