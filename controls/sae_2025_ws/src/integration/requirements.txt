# Pinned dependency set for the integration backend.
#
# Single source of truth shared by CI (.github/workflows/integration.yml) and
# launch.sh, so the tests run against the exact versions deployed to the machine
# An unpinned upgrade is what previously broke CI (FastAPI 0.137 /
# Starlette 1.x changed app.routes internals).
fastapi[standard]==0.128.7
starlette==0.52.1
httpx==0.28.1
python-multipart
pydantic>=2,<3
pyyaml
zeroconf
pytest
