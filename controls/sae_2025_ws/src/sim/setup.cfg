[develop]
script_dir=$base/lib/sim
[install]
install_scripts=$base/lib/sim
[tool:pytest]
markers =
    live: requires real hardware or a running peer stack; excluded from default CI
junit_suite_name = sim
