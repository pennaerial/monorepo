world:
  name: in_house_2026
  node: in_house_2026_node
  config:
    # Each patch has a fixed 4 m radius and 16 shapes, including 4 gold stars.
    num_patches: 3
    seed: null                    # Set an integer to reproduce a layout.
    area: [[-30, -30], [30, 30]]   # World XY bounds in meters; whole patches fit inside.
    min_spacing: 1.0              # Minimum shape spacing and gap between patch edges.
    keep_out: [[0, 0, 10.0]]        # (x, y, radius) in meters; keep patches clear of the UAV.
