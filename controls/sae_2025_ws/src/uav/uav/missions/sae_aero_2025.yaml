start:
  class: uav.autonomous_modes.NavGPSMode
  params:
    coordinates: '[((5, 3, -10), 3, "LOCAL")]'
  transitions:
    complete: payload

payload:
  class: uav.autonomous_modes.FindAndLandMode
  params:
    color: 'yellow'