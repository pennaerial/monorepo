start:
  class: uav.autonomous_modes.NavGPSMode
  params:
    coordinates: '[((5, 10, -10), 1, "LOCAL"),]'
  transitions:
    finished: payload

vision:
  class: uav.vision_nodes.PayloadTrackingNode

payload:
  class: uav.autonomous_modes.PayloadPickupMode
  params:
    color: 'blue'
  transitions:
    complete: land

land:
  class: uav.autonomous_modes.LandingMode