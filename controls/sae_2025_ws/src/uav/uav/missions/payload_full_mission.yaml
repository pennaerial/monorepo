modes:
  start:
    class: uav.modes.payload.PayloadWaitForDriveOutMode.PayloadWaitForDriveOutMode
    params:
      dark_threshold: 100
      clear_ratio: 0.3
      wait_seconds: 15.0
      turn_angular: 3.14
      turn_speed: 1.5
      compressed_image: false
      debug: true
    transitions:
      complete: corner
