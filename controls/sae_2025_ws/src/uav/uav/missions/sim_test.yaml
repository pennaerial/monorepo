takeoff:
  class: uav.autonomous_modes.VerticalTakeoffMode
  transitions:
    loop: takeoff
    failure: error

gps:
  class: uav.autonomous_modes.NavGPS
  params:
    coordinates: [((2, 3, 2), 5), ((2, 3, 2), 3)]
  transitions:
    loop: gps
    failure: error

land:
  class: uav.autonomous_modes.LandingMode
  transitions:
    loop: land