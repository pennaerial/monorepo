modes:
  start:
    mode: uav.vtol.TakeoffMode
    params:
      takeoff_type: vertical
    transitions:
      complete: GPS

  GPS:
    mode: uav.NavGPSMode
    params:
      coordinates:
        - [[0, 0, -3], 5, LOCAL]
        - [[0, 0, -6], 5, LOCAL]
    transitions:
      complete: land

  land:
    mode: uav.LandingMode
